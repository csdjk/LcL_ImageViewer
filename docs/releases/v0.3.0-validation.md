# v0.3.0 发布前验证

用户明确授权：更新README、重拍配图、打包并发布新版本。版本由0.2.0升级到0.3.0；现有本地提交全部保留，不强推、不改写旧版本。

## 分支验证

- workspace测试40项通过，check和release workspace构建通过。
- imageview.exe的FileVersion/ProductVersion为0.3.0，x64且GUI子系统；iv_shell.dll为x64，可加载且导出DllGetClassObject/DllCanUnloadNow。
- Inno Setup 7.1.0便携编译器官方SHA256核验通过，安装包编译通过；ZIP CRC及包内文件哈希核对通过。
- README共5张实际程序配图：深色、浅色、Alpha单通道、紧凑设置和无分类菜单。输入为仓库自身图标；桌面磨砂关闭，截图不显示个人路径和桌面内容。
- 截图源码d2e0662；原始证据保留于忽略目录ui-verify-shots/readme-v0.3.0-worker，每张有commit/输入hash/程序hash/DPI/客户区元数据。公开图片只作无损裁剪或JPEG转码，没有重绘UI。
- README修正旧文档的Mip快捷键和不存在的F11声明；打包脚本提供版本清单和SHA256SUMS。
- 安装器及主程序未配置代码签名；iv-shell保留LNK4104导出可见性警告，不影响本轮DLL加载检查。未将历史全仓rustfmt差异混入此任务。

## 边界

本轮不在用户现有安装上执行安装/卸载，不修改文件关联或缩略图注册，不关闭用户窗口。P1多DPI、跨屏、完整性能与系统集成验收仍保留，不因发布而伪标通过。主线包与发布地址在完成验证后追加。
