# v0.4.0 发布验收

## 正式发布

发布页：https://github.com/csdjk/LcL_ImageViewer/releases/tag/v0.4.0。Release ID `389994098`，发布于 `2026-09-16T14:18:52Z`；Latest、非草稿、非预发布。代码标签 `v0.4.0` 对应 `1649569a903e3620b60f3699ea0f89c176900014`。旧标签和附件保留，未强推或覆盖。

本轮按用户明确授权，将原有41个本地提交及本次版本、打包工具更新推送main。源标签包含动态AVIF、子目录浏览/相对路径、透明画布/图片边界和缩略图修复；README按当前功能组织，CHANGELOG与发布说明同步。

## 构建与依赖

正式EXE与DLL在独立 `target/distribution/x86_64-pc-windows-msvc/release` 构建，使用 `-C target-feature=+crt-static`；不覆盖开发目录或用户运行中的程序。PE导入表逐项验证，没有额外的VC运行库、AVIF/AOM/dav1d DLL依赖。普通看图不需注册系统编解码器。

EXE：`fc954f452b43ed81fb592ad0bbb373a71458191855ac3fda72c38a3630b9ca08`，25,435,648字节。
DLL：`8f8b01ebf6d4422c72131200260c6892a05951c1c9489e7db895d030c448618b`，15,650,304字节。
版本资源、Cargo workspace及lock、安装器均为0.4.0。构建证明：`target/distribution/x86_64-pc-windows-msvc/release/release-build.json`。

```powershell
# Rust MSVC、CMake、NASM应在当前构建进程的PATH可见。
cargo test --workspace --locked
cargo check --workspace --locked
python -B -m unittest discover -s tools/tests -p "test_*.py" -v
python -B tools/build_windows_release.py --target-dir target/distribution
python -B tools/package_release.py --iscc <ISCC.exe路径> --binary-dir target/distribution/x86_64-pc-windows-msvc/release --output dist/v0.4.0
```

打包工具拒绝脏源码、版本/构建证明/hash不符、外部运行库依赖及非空输出目录；重复验证应使用新输出目录。便携包只收录显式白名单，并将 `licenses/AVIF-third-party-notices.txt` 纳入ZIP与version.json清单；安装包同样带许可证。

## 实际验证

- 主线101项Rust回归、18项安装与打包契约检查、workspace check通过。
- 正式Release共146张通过验证的截图：动态AVIF深色1280×860和浅色880×560，自动播放/暂停/逐帧/宽屏进度条/恢复，RGBA/Alpha/R与独立PNG参考比对；子目录相对路径、透明画布与边界、WebP/PSD同画面、菜单/设置与隐藏恢复，以及ZIP解压后启动。DPI96/100%；已抽查关键截图。三次额外全新进程自动播放复验均观察到多个不同动画帧。
- 直接COM加载正式DLL：16个AVIF/PNG输出及12个WebP/PSD/PNG/JPEG输出，共28个缩略图；透明度和可见像素对比通过，不执行真实COM注册或重启Explorer。
- 真实Inno隔离测试：76个类型明确的注册值、AVIF/WebP/PSD标准缩略图GUID、旧空键与旧失效路径升级、其他应用哨兵保留、EXE/DLL安装hash及卸载均通过。注册键重定向至独立HKCU测试命名空间；不等于完整生产环境“打开方式”菜单测试。
- 默认D盘及无D盘回退选择在无安装负载的真实Inno探针通过。用户当前安装、实时关联、UserChoice及偏好保持不变。
- 安装包和ZIP内EXE版本核对、ZIP CRC、逐文件manifest/hash、许可文件及解压后实际启动通过。发布附件由GitHub返回digest核验后再公开，无鉴权公开下载三文件逐项SHA256一致，Latest再次回读确认。

首次深色自动播放采样在脚本动作前已处于暂停状态，原因未确认，未计入通过截图数。保留失败证据 `ui-verify-shots/release040-animated-dark`；未修改业务代码，同一二进制完整重跑通过，并补做三个独立新进程自动播放检查均通过。不把这次重跑描述为已修复一个确认的程序缺陷。

## 附件

| 文件 | 字节 | SHA-256 |
|---|---:|---|
| `LcL-ImageViewer-Setup-v0.4.0-win64.exe` | 9,334,163 | `4674e483e50df9780c0045e22a47cceaf37aa609e03a986d4cde99871e05cba6` |
| `LcL-ImageViewer-v0.4.0-win64.zip` | 14,663,448 | `8f078f56c40af0b97c1e04e22828adfbbd4361881352aa510f07671b1fcb4b37` |
| `SHA256SUMS.txt` | 206 | `ffb0e477711734452112c0018623354671878cf77ff95d77efb6376ff617a2a3` |

本地正式包：`dist/v0.4.0/`。安装版使用原AppId并在升级时沿用目录；用户升级前关闭旧程序。此次不自动执行真实安装，不更改默认打开方式。

## 证据与限制

`Temp/release040-main-{tests,check,contracts}.log`、`Temp/release040-static-build.log`、`Temp/release040-registry/report.json`、`Temp/release040-directory/report.json`、`Temp/release040-verification.json`、`Temp/release-v0.4.0-published.json`、`ui-verify-shots/release040-*`。截图/日志位于忽略目录，仅本机保留；公开仓库保存可重复入口和文字结果。

未签名；其他DPI、跨屏、全部生产文件变体及完整安装环境矩阵未覆盖。AVIF HDR/ICC、源文件有限循环次数停止仍不支持；整段RGBA256MiB预算不等于进程总内存上限。P1保持ACTIVE，后续NEXT回到IV-P1-01，不将本轮发布等同于所有长期门禁通过。
