# v0.3.1 发布验收

## 来源与范围

用户要求更新安装包并发布。功能基线为 `dfb45de`，本次版本集成为 `aa70310ba56e769ff14646c04ae73162757bd0ed`。主程序逻辑相对已验证加载优化基线无新增改动，仅统一workspace和安装器版本为0.3.1；整合已完成的PERF-01和INSTALL-01/02。保留旧标签和公开附件。

## 本次已验证

- 主线 `cargo test --workspace --offline --locked`：54项通过；workspace check、Debug主程序及Release workspace构建通过。
- `python -B -m unittest discover -s tools/tests -p 'test_*.py'`：11项安装器配置/注册/默认目录检查通过。
- 真实Inno隔离注册安装：复现旧脚本空值和残留路径，修正版71个字符串值、其他应用哨兵保留、卸载自身值均通过。此次额外复制本次0.3.1 EXE/DLL到隔离安装目录，逐文件SHA256一致，卸载后载荷清理通过。
- 实际运行Inno目录解析器：有D盘选D:\Program Files\LcL ImageViewer；缺盘分支注入测试回退目录；未卸载或改变真实磁盘。目录选择和升级策略另有配置检查。
- 正式Release实机：深色880×560设置弹窗16状态、浅色1280×860菜单10状态通过。DPI96/100%，已查看新程序设置页v0.3.1及无分类菜单截图；图片内容、A/D、关闭/重开、弹窗独立拖动、原生画布右键拖窗、删除确认取消、自动隐藏恢复保留。
- 真实用户关联快照与普通偏好保持不变；未关闭用户已有程序或改动安装目录。

## 当前二进制

- `target/release/imageview.exe`：`0b6ce0d06bac1927a7a5475eafef3fe8bb22e63d9ac9eb7e56a1877157fc5bf1`。
- `target/release/iv_shell.dll`：`226d7cfa38368b5960aae53b676f26e2f30c011aa152dcf82c0db22015377337`。
- EXE资源版本已核对0.3.1，Debug同步。保留原有iv-shell LNK4104导出警告。

## 证据与重复命令

本次输出：`Temp/release031-main-payload/report.json`、`Temp/release031-main-directory/report.json`、`Temp/release031-qa-summary.json`，及 `ui-verify-shots/release031-popup-dark-880`、`release031-menu-light-1280`。上述目录均为本地证据，不进入发布包。

```powershell
python -B tools/tests/installer_registry_smoke.py --iscc <ISCC.exe> --baseline 8fc0d51921cbcedce4aaf6827970409ef92ba6a9 --payload-dir target/release --output <新的隔离目录>
python -B tools/tests/installer_default_dir_smoke.py --iscc <ISCC.exe> --output <新的探针目录>
python -B tools/package_release.py --iscc <ISCC.exe>
```

## 边界

隔离注册测试通过不等于在当前用户真实“打开方式”菜单执行升级验收；未自动安装正式包、修改UserChoice或重启Explorer。未增加全DPI/跨屏/全部格式组合的覆盖。完整安装向导截图、无权限D盘、完整生产升级/卸载全矩阵未验证。安装包未配置代码签名。此前性能基准只适用于已注明的样本/缓存/CPU绘制提交边界，不在发布中新增速度保证。

## 打包与公开下载

正式包基于本记录后的文档提交打包；在发布前逐一核对版本、ZIP CRC和内容hash，并从解压程序执行启动验证。上传后的Release编号、附件大小/hash和公开下载复核记录由发布收尾写入main文档；以实际GitHub回读为准。
