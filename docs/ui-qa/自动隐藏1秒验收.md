# 1秒自动隐藏：已完成

随N03集成 `6de1d75f8b9ccb511246f61efc6efeaef3c96f9d`，main tests21/check/release及双主题双尺寸实机隐藏/恢复验证通过。显式QA隔离profile解除旧窗口占用的验收阻塞，用户旧Debug及偏好保持不变。详细证据见 `两侧玻璃导航验收.md`。

## 历史阻塞记录（已解除）

# 菜单栏 1 秒自动隐藏 — 当前验证记录

Task: IV-P1-N02。源码提交：`a23f07dff8cd1f2a5e1f5c85830a667313e34cdb`。

## 改动

`OVERLAY_HIDE_DELAY = Duration::from_secs(1)`；活动判定和定时重绘共用此值。静止1秒后开始淡出，沿用120ms淡入/180ms淡出；鼠标移动恢复、按住鼠标时保持显示、上下栏共用计时的既有行为不变。没有修改新拟态样式或图片功能。

## 已验证

- `cargo test --workspace --offline`：16 passed / 0 failed，包含999/1000/1001ms边界以及按住鼠标、移动恢复、无图底栏状态测试。
- `cargo check --workspace --offline`：PASS。
- `cargo build --release -p iv-viewer --offline`：PASS。
- 新版产物：主工作区 `target/release/imageview.exe`，SHA-256 `17afe8948da9af871ee407cc8abbfd1cc1c8fcae56870850d58fe5a3f65a219b`。
- `git diff --check`：PASS。

## 当前阻塞（未完成项）

用户仍在运行主工作区 `target/debug/imageview.exe`。Debug构建因旧进程占用返回os error5；没有强制关闭用户窗口。实机截图工具为避免覆盖其他运行实例的偏好而拒绝并行运行，因此尚未执行本次1秒隐藏/恢复的实机复验，也尚未将功能分支合入main。

旧Debug程序尚未更新，新Release程序已经编译。本任务保持BLOCKED而不是DONE。

## 恢复

关闭正在运行的查看器后，先确认main与本任务worktree均干净、无并发修改。使用本任务 `tools/ui-qa/autohide-actions.json` 及既有 `neumorphic_smoke.py`，对light/dark和1280×860/880×560分别验证静止不足1秒可见、1秒后淡出、移动恢复、按住超过1秒仍可见、松开后隐藏。每次指定当前构建commit和独立输出目录，记录真实截图元数据并检查配置恢复。

通过后记录结果，提交任务分支、安全集成本地main并复验；重新构建Debug，保留已验证的Release源码/哈希对应关系，更新协调状态。不要重做UI、不覆盖用户修改、不push或发布。
