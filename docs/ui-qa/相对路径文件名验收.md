# 子目录图片相对路径标签（IV-P1-N12）

## 行为

顶部文件名相对于当前固定浏览根目录显示。根目录中为 `billboard.gif`，子目录中为 `fx\billboard.gif`，多层目录保留各层相对路径；关闭包含子文件夹后随新的浏览根恢复为文件名。标签继续使用原有固定宽度和中间省略，不挤压其他按钮。悬停第一行是完整标签，下一行为真实文件路径。

路径展示借用已有 Path，只有文本转换，不增加扫描、metadata 或 canonicalize 操作；既有图片缓存、导航和原生窗口标题不变。没有新增快捷键、开关或偏好。

## 验证

源码 `7d59cb18a8ccf6c88881bbbf39055658842411db`，Release SHA256 `eb2f7e1a56af26dbe5ee61e87ed62d666b97fe7c75d0f662e92724ecb2976820`。73项workspace测试及check、Debug/Release构建通过；新增4项测试覆盖根目录/多级目录、中文空格带点目录、重新定根、范围外与父路径回退。

工作分支真实深浅主题、880×560与1280×860共44张截图，测试图片仅为本次新建的3个同名GIF。实际检查子目录标签变化、关闭范围恢复文件名、完整悬停、长中文路径省略、菜单、自动隐藏与恢复；图片和用户偏好未改动。所有截图DPI96。

重跑命令：

```powershell
python -B tools/ui-qa/relative_path_smoke.py --binary target/release/imageview.exe --output ui-verify-shots/<新目录> --commit <源码提交> --theme dark --width 1280 --height 860
```

证据：`ui-verify-shots/n12-dark-1280`、`n12-light-880`、`n12-light-1280`、`n12-dark-880`。其他DPI尚未覆盖；不是所有图像格式重测。本次只改标签与验证资料，不更新安装或关联，不推送或发布。

主线集成 `bf37c8cc83268808bcddf7b20af956d537841194` 后73项workspace测试、check、Debug/Release构建通过；当前主线Release实际深色1280×860与浅色880×560共22张截图复验通过。相对标签与关闭恢复文件名的像素比较、长路径悬停、菜单/隐藏恢复通过。证据为 `ui-verify-shots/n12-main-dark-1280` 与 `ui-verify-shots/n12-main-light-880`。

当前Release SHA256 `6f3ec85c08600cfdaec1c8db9bb7b9400f94df9a13cad4f80b7c1b21f198d166`，Debug SHA256 `8d5043581ec6d25f85307fa0bfc4416e6eb050ba44e28f5aaaedfffc275a6900`。常规工程可执行文件已更新；现有安装、v0.3.1发布包和远端未变。
