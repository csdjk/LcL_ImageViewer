# 紧凑设置页与标题拖窗验收（IV-P1-N08）

## 实现范围

设置页面改为单层列表，常规行高40点；移除外观、新拟态材质、画布背景、Windows集成等分类卡片及装饰性说明。保留主题、透明背景、减少动效、桌面磨砂、打开方式和默认应用入口；帮助说明改为悬停提示，页脚仅名称与版本。磨砂开启时才显示三条32点参数行，原参数范围、持久化键和错误诊断保留。

设置标题及标题内空白区独立接收左/右键拖动，复用当前进程WindowDrag的屏幕物理坐标差；移动整个原生窗口而非内部面板。关闭按钮与拖动区隔开8点，正文、开关和滑条不触发拖窗。松键、失焦、最大化或原生API失败结束操作；Esc可取消正在进行的标题拖动。未改解码、Shader、回收站、系统关联实现及原有其他鼠标手势。

## 工作分支验证

最终源码：`3c89b4729cc8891b287fe6d764895da3b077a3e8`。

- `cargo test --workspace --offline`：37 passed / 0 failed；`cargo check --workspace --offline`、Debug与Release构建PASS。
- Release SHA256：`c9b3888b36a68a735a1976f5b0dd274d737aaf65e7bd42463cbcdf960449f255`。
- Debug SHA256：`3c22dd5ad42c43ac65b5bdb2758f518da82d4e5bda740fe4f4fee41bad13c350`。
- `ui-verify-shots/settings-stable-{light,dark}-{880,1280}`：4组、各26张真实Windows应用截图，共104张；DPI96/100%，880×560及1280×860。
- 四组的标题左键、标题右键、顶部空白左键实际窗口位移分别为72×36、-48×24、-36×-24，大小不变；正文/开关/两条滑条拖动窗口位移均为0。
- 真实点击主题切换、减少动效与透明背景选择、磨砂开/关及滑条操作、关闭设置、A/D、Delete确认取消、1秒隐藏恢复通过。测试保存配置确认透明度0.64、模糊强度0.33，其余测试偏好恢复到初始值；测试输入文件SHA256不变。
- 已检查当前构建深浅主题默认/展开截图：无分类大卡片、操作右对齐、控件无裁切；源码修复了最初嵌套水平布局导致滑条轨道被挤压的问题。

## 可重复入口

```powershell
python -B tools/ui-qa/compact_settings_smoke.py --binary target/release/imageview.exe --output ui-verify-shots/settings-new-light-880 --commit <实际源码提交> --theme light --width 880 --height 560
```

入口仅操作本次启动的PID与新建隔离QA profile。按钮点击使用SendInput批次，拖动使用实际鼠标轨迹；保留前台窗口身份检查。磨砂短暂截图排除时有上限重试，只等待程序自行释放，不修改系统截图权限。截图、轨迹、主题、窗口外框、DPI、程序/输入hash与测试配置输出在忽略目录。测试不执行注册/注销/默认应用，不确认删除图片，不操作用户原图。

早期因前台焦点被打断、截图暂时不可用及滑条布局问题产生的失败轮次均保留，不计入最终成功数量。定向窗口消息试验未通过，被保留在Temp，不作为验收入口或成功证据。

## 主线复验

main集成 `d3299f57c1c0680beb2cfc4eec884a4239d62445`；37tests/check/Debug/Release通过。当前Release SHA256 `e34ec44c7c3a67adc09fd16e75bb0bd9468dfec9b9520d99cbf9fc8636a08ef7`，Debug `28b410f41e32053c515dba1aedda9fa27fe672f91fa9af165c1c56726d04591c`。

主线 `ui-verify-shots/settings-main-light-1280` / `settings-main-dark-880` 各26张实际截图，共52张，几何、保存参数、原图不变检查均PASS；两个报告的commit/hash与当前程序一致。三次标题移动准确为72×36、-48×24、-36×-24，其他控件拖动窗口位移为0。已实际查看主线深色880默认和磨砂展开截图，布局完整、无分类卡片、滑条可见。

另补齐N07专门主线10张回归（`flat-menu-main-finish-dark-880`），无分类菜单、属性、确认取消、A/D和隐藏恢复PASS。主线测试前后正常用户配置SHA256一致。两个常规构建路径均更新，未关闭用户窗口、未修改关联或推送。

## 限制

未覆盖其他DPI、跨屏、全部最大化/全屏手势、桌面捕获性能/恢复全矩阵及完整辅助功能。文件关联按钮仅检查布局，未执行系统写入。无push、安装器或远端发布，不将P1标记为VALIDATED。
