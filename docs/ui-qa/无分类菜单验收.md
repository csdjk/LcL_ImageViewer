# 无分类右键菜单验收（IV-P1-N07）

## 调整

移除“图像检查”分类标题和三处分组空白，菜单成为连续单列。复制像素操作统一32点行高，与其他菜单项一致。移除已无调用的menu_section/menu_sep辅助函数。
保留所有操作回调与A/D、Delete等快捷键；未修改删除、文件操作、导航、鼠标手势、图像解码或Shader。现有菜单回归脚本的点击坐标已同步。

## 验证

源码12d168e：workspace35tests/0失败；check和Debug构建通过，无新增编译警告。
`ui-verify-shots/flat-menu-verified-{light,dark}-{1280,880}/`四组各10张实际Windows截图（DPI96）：含/不含复制像素的单列菜单、悬停、属性窗口、删除确认/取消、A/D、隐藏恢复；新建测试图hash不变，没有确认删除或回收任何文件。
已查看浅色880无像素行、深色1280含像素行截图：无标题、无组间空白、操作间距一致、快捷键对齐正常。旧首轮截图因右键期间指针移动变成拖窗而无效，不计成功；测试点击改为连续按下释放再重验通过，不改变产品右键手势。

## 重复运行

```powershell
python -B tools/ui-qa/flat_menu_smoke.py --binary target/debug/imageview.exe --output ui-verify-shots/flat-new-run --commit <实际源码commit> --theme dark --width 880 --height 560
```

仅新建输入/独立QA配置，不关闭已有查看器、不改正常配置；输出目录必须是新目录。完整P1、多DPI或跨屏/性能不在本轮范围。

## main复验

待合入后记录当前二进制与真实菜单复验。
