# 新拟态 UI 验收 — 2026-09-14

## 结论与构建

本轮视觉实现已在真实 Windows 桌面完成基础验收，源码基线 `62b5163d34f36b825399a7eb720e446675ec1b98`。工作分支 workspace tests **15 passed / 0 failed**，cargo check、debug 主程序和 release 主程序构建通过，最终构建未产生新的编译警告。整个 P1 仍为 ACTIVE，不把本任务等同于完整阶段验收。

Debug SHA-256：`516eea0268c3fd7245d3bc3a2a82ff88834236ef28479930a5a6e6575f994648`。

Worker release SHA-256：`db301ee646fa3ff68fd7b2c29b600c6cff59b3de302d6570dec400a4315200ee`。

本页记录工作分支验证，main 集成复验已经完成，结果见本页末尾。不得把较早迭代的截图或二进制当作最终版本。

## 实际截图覆盖

输出目录均相对主工作区的 `ui-verify-shots/`。每张原始 PNG 有独立 JSON：源码 commit、二进制与输入 SHA-256、状态、动作、逻辑/物理客户区、DPI、裁剪、时间。以下实际捕获均为 **DPI 96 / 100%**，逻辑与物理尺寸一致。

| 场景 | 目录 | 原始截图 |
|---|---|---:|
| 浅色 1280×860 | `neumorphic-final-light-1280` | 13 |
| 深色 1280×860 | `neumorphic-final-dark-1280` | 13 |
| 浅色 880×560 | `neumorphic-final-light-880` | 8 |
| 深色 880×560 | `neumorphic-final-dark-880` | 8 |
| DDS 打开与窄窗布局 | `neumorphic-final-dds-880` | 1 |
| 长中文文件名中间省略且保留扩展名 | `neumorphic-final-long-name-880` | 1 |
| 浅色空态 | `neumorphic-matte-empty` | 1 |
| Tab 键盘焦点递进 | `neumorphic-final-keyboard` | 5 |

共 **50 张**当前源码的原始运行截图，不包括联系表、旧版基线和中间迭代。13 状态包含默认、R/G/B/Alpha、悬停、按下、Tab 尝试、菜单、设置、滚动、隐藏、恢复。窄窗包含紧凑通道下拉框。

目视检查了设置的浅色窄窗、深色窄窗、滚动状态、菜单、长文件名、DDS 以及交互局部拼接图。控件未发现越界或不可关闭；滚动区域底部内容的裁剪是预期滚动行为。第一轮单次 Tab 不足以证明焦点；补充带实际扫描码的多次 Tab 捕获后，在 `tab-3` / `tab-4` 明确看到打开/上一张按钮的语义焦点轮廓。尚未验完整键盘遍历、所有控件禁用态和屏幕阅读器。

截图脚本临时切换此应用的配置文件，所有已完成运行 `preferences_restored=true`，原始配置 SHA-256 `db74b2ab85eb268f424569aca5b3331f3c9ba7378aeade18bf29e9111dcaf826`。没有修改系统 DPI、注册表或文件关联，没有触发 Windows 集成按钮。

## 图片保真与自动隐藏

旧版构建 `6025053` 与最终源码使用相同 `gradient.png`、浅色主题、棋盘格、1280×860、DPI96。比较图片内部区域 `(240,90)-(1040,770)`，RGB 平均绝对误差与最大误差均为 **0**。这仅证明该固定测试图内部区域一致，不是整张 UI 对设计稿的 100% 相似声明，也不替代所有格式/色彩模式验证。

四个主题/尺寸组合中，自动隐藏后恢复的顶部区域与初始顶部区域 RGB 差异均为 0；隐藏帧与初始帧确有差异。原始比较数据见 `neumorphic-review/comparison.json`，差异图 `image-interior-diff.png`。Shader、解码和原始像素采样源码未修改；app 仅对不透明 UI 关闭无必要的玻璃采样区域。

## 本轮闭环

1. 初次外阴影出现发光边缘，定位为此 egui-wgpu 0.27 gamma 混合后端与线性预乘彩色 Alpha 不匹配；改用同步 gamma 缩放 RGB/Alpha，并新增暗影混合不增亮测试。修复后的真实空态与组件呈现凸起/凹陷而非发光轮廓。
2. 设置卡片阴影按扣除 outer margin 的真实边界绘制，避免分组阴影侵入外部留白。
3. 清除新代码的数值类型兼容警告和无用字段；只格式化本轮 UI 文件，不格式化历史整个仓库。

## 未覆盖 / 不作承诺

没有执行 125%/150%/200% DPI、1920×1080 矩阵、HDR/动画/全格式、所有禁用态、错误/加载状态、桌面捕获失败恢复、最大化/全屏/最小化、完整键盘与辅助功能、CPU/GPU 性能采样。DDS 只验本次样本加载和窄窗布局，不据此声称所有 Mip 交互均通过。没有目标设计稿，因此不声明 98% 设计稿相似度；没有计时数据，因此不声明 GPU ≤2ms 或固定帧率。后续由原 IV-P1-01/02/03 补齐。

## 复现

```powershell
cargo test --workspace --offline
cargo check --workspace --offline
cargo build --release -p iv-viewer --offline
python -B tools/ui-qa/neumorphic_smoke.py --binary <当前imageview.exe> --output <全新忽略目录> --input <gradient.png> --commit <源码commit> --theme light --actions tools/ui-qa/standard-actions.json
```

1280×860 使用 `standard-actions.json`，880×560 使用 `narrow-actions.json`，焦点专项使用 `focus-actions.json`。动作坐标已与本轮固定输入截图核对；替换布局、文件或 DPI 时重新检查，不把脚本坐标当作永久定位器。

## Main 集成复验完成

集成提交 `635b7dcc195c5c220882cb9589b3762ebc124eff`。在main重新执行workspace tests（15/0）、check、release主程序构建，全部通过。重建后的Release SHA-256：`ec37f483172dad922561aee51b097b6c5a432d200541ad908f95b8cb55483256`。

`neumorphic-main-release-light` 13张、`neumorphic-main-release-dark-880` 8张、`neumorphic-main-release-empty` 1张，共22张Release原始截图；二进制hash、commit、DPI96及1280×860/880×560尺寸逐张核对，三个运行的配置恢复均为true。结合工作分支50张，共72张最终源码/集成源码的原始截图，不计联系表或中间迭代。

已验证原用户main提交 `1068627` 仍为集成提交祖先；没有重写原有历史，没有push、发布、注册表或文件关联操作。最终只追加协调与验收文档，不改变已验证源码。
