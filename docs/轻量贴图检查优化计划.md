# 轻量贴图检查优化：首轮执行合同

Task：IV-TA-01。用户于 2026-09-21 授权执行上一轮建议的首轮三项。
Base：`6934031`；Owner：Codex；分支：`codex/ta-foundation`；工作树：`Temp/worktrees/ta-foundation`。

## 范围与结果

1. 修正 DDS DX10 BC6H 标识及相邻 BGRA/BGRX 格式识别，用手工构造的标准文件验证。验收时进一步发现标准 Mipmap 标志和 Volume 各级切片偏移错误，一并修正。保留既有 BC6H 8 位预览边界，并在属性中明确说明；不将预览声称为浮点 HDR 保真。
2. GIF/APNG/WebP 与 AVIF 使用一致的 256 MiB 保留帧像素预算，包含单独首帧；超预算、超帧数和异常尺寸明确失败，不能静默截断成成功。此预算不包含解码器临时内存，也不是进程总内存上限。
3. 增加当前文件保存后自动刷新、F5 手动刷新及视图锁定。刷新保留缩放、平移、通道、有效 Mip 和曝光；视图锁定额外应用于切图。同路径刷新过程中保留上一有效图像直到新结果成功，失败明确提示。连续保存合并，过期解码结果不得覆盖更新版本或其他文件。自动刷新与视图锁定可设置并持久化。

## 轻量与边界

- 不增加第三方依赖；沿用 Rust/egui/wgpu、单解码线程和有界结果队列。
- 后台只检查当前文件的元数据，合并连续变化；未变化不唤醒界面，不扫描目录树，不读取全部文件计算 hash。
- 16 位源数据保留、完整 ICC/线性色彩管理、Alpha 诊断、法线/平铺、双图对比、序列帧工具属于后续阶段，本轮不改像素存储与渲染语义。
- 不 push、发布、分发安装包、更新用户安装或修改关联；不覆盖旧构建和用户文件。

2026-09-23 用户随后明确追加新版本打包发布授权，发布作为独立 `IV-REL-060` 执行；本 Task 的实现与验收边界保持不变。

## Allowed Paths

业务：`crates/iv-core/src/{dds.rs,decode.rs,avif.rs}`、`crates/iv-core/tests/**`、`crates/iv-viewer/src/{app.rs,loader.rs,main.rs,file_watch.rs}`、`README.md`、`tools/ui-qa/**`、`docs/ui-qa/**`、`docs/performance/**`。
协调：由 Coordinator 在 main 更新 `docs/{status.md,roadmap.md,task-board.md,development-plan.md}` 及本文。
本地产物：`Temp/**`、`target/**`、`ui-verify-shots/**`。

## 验收

- DDS BC6H UF16/SF16、BGRA/BGRX sRGB/UNORM、未知类型/截断输入与原格式回归。
- 总帧字节预算包含首帧、精确边界/溢出/超帧数/尺寸变化失败；原动画像素与时序不变。
- 自动刷新、原子替换、连续保存、手动刷新、失败后恢复、切图取消旧请求的测试；确定性验证同路径旧结果不会覆盖新结果。
- workspace tests、workspace check、相关配置检查、release 主程序构建、diff 检查。
- 当前 Windows 构建截图：深浅主题，880×560、1280×860；设置开关、默认/悬停/按下/选中/焦点、自动隐藏/恢复、右键菜单、刷新前后视图与像素、失败提示、长中英文路径。截图记录源码、二进制、输入、DPI和客户区。
- 记录主程序大小与同配置基线，检查空闲唤醒/内存边界；没有实测的性能不作结论。
- 任务分支提交、审查、串行合入 main；主线必要复验、同步文档后才 DONE。
