# 更新日志

## v0.5.1 — 2026-09-20

- 左右切图按钮由竖向圆角矩形调整为 **56×56 圆形按钮**，玻璃模糊、描边和阴影同步圆形化。
- 保留原有点击切图、禁用状态、悬停反馈及 1 秒自动隐藏/恢复行为。
- 延续 v0.5.0 的 RGBA 通道、动画逐帧、AVIF、背景调色板、置顶和资源管理器缩略图等功能。

## v0.5.0 — 2026-09-20

- 顶部新增窗口置顶开关，保留选择，切图和最小化恢复不改变置顶状态。
- 背景切换移至顶部：棋盘格、跟随主题、黑白灰与自定义颜色。
- 自定义颜色改为二维调色板、色相条、HEX输入与快捷色块，实时预览；不修改图片与通道数据。
- 修复置顶文件对话框层级及Esc取消时意外退出查看器的问题。
- README补齐各项主要功能的实机截图，加入RGBA通道及动画播放、暂停、逐帧操作演示。
- 保留AVIF动画、透明通道、子目录浏览和资源管理器缩略图功能。

## v0.4.0 — 2026-09-16

- **AVIF 静态及动态图片**：支持透明通道，动画自动循环播放；可播放/暂停、前后逐帧，宽窗口可拖动帧进度。保留每帧时长、Alpha，以及容器裁剪、旋转和镜像。
- **子文件夹浏览**：顶部开关或 `S` 开启，只向当前目录下查找；左右按钮、A/D 与方向键可跨子目录切图。顶部显示图片相对于浏览根目录的路径，悬停查看完整路径。
- **透明图检查**：棋盘格铺满画布；顶部按钮或 `B` 高亮图片完整边界，包含透明留白并跟随缩放、平移。
- **缩略图修复**：补齐 WebP、AVIF 缩略图支持，修复 PSD 缩略图注册与部分附加通道文件的读取；动画缩略图仅解码首帧。
- **稳定性**：修正动画计时的累计误差；目录扫描可取消且有资源限制，AVIF 动画按总解码数据量检查预算。
- **用户文档与打包**：README 按当前功能整理；安装包与免安装包包含新增解码库的许可文件。



## v0.3.1 — 2026-09-15

- 加载优化：首图提前后台解码，与窗口/GPU初始化并行；目录异步扫描，同目录导航复用列表；最新请求优先，预读去重有界，减少像素缓冲拷贝。
- 修复静态WebP误入动画分支而报无有效帧的问题。
- 修复安装器“打开方式”只创建空注册键的问题，显式写入71个字符串值并通知Explorer刷新；不抢占默认关联。
- 全新安装默认D:\Program Files\LcL ImageViewer；无D盘时回退用户目录，升级沿用原位置，目录可手选。
- 安装包与便携包统一版本0.3.1；保留v0.3.0发布文件。


## v0.3.0 · 2026-09-15

### 界面


- 全面更新深浅主题：雾蓝灰新拟态控件、柔和阴影与选中/按下反馈。
- 上一张/下一张移至窗口两侧，使用半透明场景模糊材质。
- 悬浮工具栏静止1秒后开始淡出，移动或点击后恢复。
- 右键菜单和设置页改为无分类紧凑列表，保留常用功能。
- 设置弹窗支持标题左/右键独立拖动；主窗口保持不动，并限制弹窗在客户区内。

### 操作与修复

- 新增 A/D 切图，保留左右方向键；画布左/中键拖图、右键拖动主窗口。
- 修复 Delete 无响应：确认后将当前图片移入回收站，取消不修改文件。
- 删除后同步导航与缓存；显示下一张、末尾回退或回到空态。不提供永久删除回退。
- “打开所在文件夹”直接打开当前图片父目录，改善中文、空格等路径处理。
- 设置开关、滑条和关闭按钮不再误触发拖动；输入框和弹层有快捷键保护。
- README 配图更换为本版本实际运行截图；纠正 PgUp/PgDn 为切图、上下方向键为 Mip 的说明。
- 免安装包缩略图脚本可直接找到同目录 DLL，解除注册时仅移除本扩展占用的处理器。

### 下载

- `LcL-ImageViewer-Setup-v0.3.0-win64.exe`：Windows x64 安装版。
- `LcL-ImageViewer-v0.3.0-win64.zip`：免安装版，解压后启动 `imageview.exe`。
- `SHA256SUMS.txt`：上述文件的 SHA-256 校验值。

升级前关闭旧版本。不要将开发目录或旧安装目录中的其他 EXE 误认为已升级。

### 验证与已知范围

本次在 Windows 100% 缩放下执行 workspace 测试、Release 构建与真实窗口回归；配图使用项目自身素材。设置弹窗拖动、无分类菜单及A/D导航已覆盖深浅主题和两种视口尺寸。

其他 DPI、跨屏、全部回收站设备/网络路径、HDR/动画全矩阵、桌面捕获性能和完整安装/卸载系统集成场景未逐项覆盖。此次按用户授权发布已验证功能，不等同于所有长期质量门禁已完成。


## 旧版本

[v0.2.0](https://github.com/csdjk/LcL_ImageViewer/releases/tag/v0.2.0) · [v0.1.0](https://github.com/csdjk/LcL_ImageViewer/releases/tag/v0.1.0)
